{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Build and Run",
      "command": "nim",
      "args": ["c", "-o:bin/${fileBasenameNoExtension}", "${fileBasename}"],
      "options": {
        "cwd": "${workspaceRoot}"
      }
    }
  ]
}
